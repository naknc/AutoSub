[project]
name = "AutoSub"
version = "0.1.0"
description = "Simple VLC-based media player with subtitle downloader"
requires-python = ">=3.10"
dependencies = [
  "PyQt6>=6.6",
  "python-vlc>=3.0.20123",
  "openai-whisper>=20231117",
  "requests>=2.28",
  "python-dotenv>=1.0",
  "websockets>=16.0",
]

[tool.uv]
